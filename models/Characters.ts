import { z } from "zod";

// const weeklyBossSchema = z.object({
//   rewards: z.array(
//     z
//       .object({ test: z.string() })
//       .or(z.object({ name: z.string(), level: z.string(), icon: z.string() }))
//   ),
// });
